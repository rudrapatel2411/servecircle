{
  "name": "servecircle-root",
  "version": "1.0.0",
  "description": "ServeCircle Root Project",
  "scripts": {
    "install:all": "npm install && cd server && npm install && cd ../client && npm install",
    "dev": "concurrently -n \"SERVER,CLIENT,TUNNEL\" -c \"cyan,magenta,yellow\" \"npm run dev:server\" \"npm run dev:client\" \"npm run dev:tunnel\"",
    "dev:server": "cd server && npm run dev",
    "dev:client": "cd client && npm run dev",
    "dev:tunnel": "node server/tunnel.js"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  },
  "dependencies": {
    "@vitalets/google-translate-api": "^9.2.1",
    "leaflet": "^1.9.4"
  }
}
